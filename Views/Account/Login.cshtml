@{
    Layout = "_LayoutLogin";
    ViewData["Title"] = "Login";
}

@model LoginViewModel

@if (Model.EnableLocalLogin)
{

    <!-- Login Form -->
    <div class="app">

        <div class="bg"></div>

        <form asp-route="Login">

            <header>
                <img src="~/login.png" style="height: 135px;" />
            </header>

            <div class="inputs">
                <input asp-for="Username" placeholder="Username or email" autocomplete="off" onfocus="this.placeholder = ''"
                       onblur="this.placeholder = 'Username or email'">
                <input type="password" asp-for="Password" placeholder="Password" autocomplete="off" onfocus="this.placeholder = ''"
                       onblur="this.placeholder = 'Password'">

                <p class="light"><a href="#">Forgot password?</a></p>

            </div>

            <footer>
                <button name="button" value="login" style="cursor: pointer;">
                    <i class="fa fa-sign-in" aria-hidden="true"></i> Login
                </button>
                <p>Don't have an account? <a href="#">Sign Up</a></p>
            </footer>

        </form>

    </div>
}
